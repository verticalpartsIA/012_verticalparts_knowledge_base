---
title: "Master Ontology VerticalParts"
layer: "VerticalParts Enterprise Ontology"
doc_type: "master_ontology"
status: "Aguardando modelagem da VerticalParts"
last_review: "2026-07-04"
llm_ready: false
rag_ready: false
graph_ready: false
---

# Master Ontology VerticalParts

## Status

Aguardando modelagem da VerticalParts.

## Princípios

- Conceitos genéricos e reutilizáveis.
- Sem regras operacionais reais nesta etapa.
- Sem processos específicos da VerticalParts antes da validação formal.
- Toda documentação futura deverá referenciar esta ontologia.

## Catálogo de Entidades

### Cliente

| Campo | Valor |
|---|---|
| nome | `Cliente` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Cliente VerticalParts |
| sinônimos | contratante, comprador |
| abreviações | CLI |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Comercial |

### Fornecedor

| Campo | Valor |
|---|---|
| nome | `Fornecedor` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Parceiro fornecedor |
| sinônimos | fornecedor homologado, vendor |
| abreviações | FOR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Compras |

### Contato

| Campo | Valor |
|---|---|
| nome | `Contato` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Pessoa de contato |
| sinônimos | interlocutor, representante |
| abreviações | CTT |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | CRM |

### Obra

| Campo | Valor |
|---|---|
| nome | `Obra` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Local de obra |
| sinônimos | site, empreendimento |
| abreviações | OBR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Instalação |

### Projeto

| Campo | Valor |
|---|---|
| nome | `Projeto` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Projeto técnico |
| sinônimos | desenho técnico, escopo técnico |
| abreviações | PRJ |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Engenharia |

### Equipamento

| Campo | Valor |
|---|---|
| nome | `Equipamento` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Ativo instalado |
| sinônimos | máquina, unidade operacional |
| abreviações | EQP |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Assistência |

### Elevador

| Campo | Valor |
|---|---|
| nome | `Elevador` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Equipamento elevador |
| sinônimos | ascensor |
| abreviações | ELV |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Engenharia |

### Escada Rolante

| Campo | Valor |
|---|---|
| nome | `Escada Rolante` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Equipamento escada rolante |
| sinônimos | escada mecânica |
| abreviações | ESC |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Engenharia |

### Esteira Rolante

| Campo | Valor |
|---|---|
| nome | `Esteira Rolante` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Equipamento esteira rolante |
| sinônimos | passarela rolante |
| abreviações | ESTR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Engenharia |

### Peça

| Campo | Valor |
|---|---|
| nome | `Peça` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Componente |
| sinônimos | item de reposição, componente técnico |
| abreviações | PCA |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Almoxarifado |

### Produto

| Campo | Valor |
|---|---|
| nome | `Produto` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Item comercial |
| sinônimos | produto cadastrado, SKU |
| abreviações | PROD |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | ERP |

### Almoxarifado

| Campo | Valor |
|---|---|
| nome | `Almoxarifado` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Depósito interno |
| sinônimos | depósito, armazém |
| abreviações | ALM |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Almoxarifado |

### Estoque

| Campo | Valor |
|---|---|
| nome | `Estoque` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Saldo de estoque |
| sinônimos | inventário, disponibilidade |
| abreviações | STK |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | WMS |

### PedidoVenda

| Campo | Valor |
|---|---|
| nome | `PedidoVenda` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Pedido de Venda |
| sinônimos | ordem de venda, venda |
| abreviações | PV |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Comercial |

### PedidoCompra

| Campo | Valor |
|---|---|
| nome | `PedidoCompra` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Pedido de Compra |
| sinônimos | ordem de compra, compra |
| abreviações | PC |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Compras |

### Contrato

| Campo | Valor |
|---|---|
| nome | `Contrato` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Instrumento contratual |
| sinônimos | acordo, contrato comercial |
| abreviações | CTR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Jurídico |

### Proposta

| Campo | Valor |
|---|---|
| nome | `Proposta` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Proposta comercial |
| sinônimos | oferta, cotação |
| abreviações | PROP |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Comercial |

### OrdemServico

| Campo | Valor |
|---|---|
| nome | `OrdemServico` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Ordem de Serviço |
| sinônimos | OS, serviço |
| abreviações | OS |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Assistência |

### Chamado

| Campo | Valor |
|---|---|
| nome | `Chamado` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Ticket de atendimento |
| sinônimos | solicitação, ocorrência |
| abreviações | CHM |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Assistência |

### Preventiva

| Campo | Valor |
|---|---|
| nome | `Preventiva` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Manutenção preventiva |
| sinônimos | visita preventiva, rotina preventiva |
| abreviações | PREV |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Assistência |

### Corretiva

| Campo | Valor |
|---|---|
| nome | `Corretiva` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Manutenção corretiva |
| sinônimos | reparo, atendimento corretivo |
| abreviações | COR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Assistência |

### Modernização

| Campo | Valor |
|---|---|
| nome | `Modernização` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Modernização técnica |
| sinônimos | retrofit, atualização |
| abreviações | MOD |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Engenharia |

### Instalação

| Campo | Valor |
|---|---|
| nome | `Instalação` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Instalação técnica |
| sinônimos | montagem, implantação |
| abreviações | INST |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Instalação |

### Entrega

| Campo | Valor |
|---|---|
| nome | `Entrega` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Entrega operacional |
| sinônimos | handover, conclusão |
| abreviações | ENT |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Logística |

### Recebimento

| Campo | Valor |
|---|---|
| nome | `Recebimento` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Recebimento físico |
| sinônimos | entrada, conferência |
| abreviações | REC |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Almoxarifado |

### Faturamento

| Campo | Valor |
|---|---|
| nome | `Faturamento` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Processo de faturamento |
| sinônimos | emissão, cobrança |
| abreviações | FAT |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Fiscal |

### NF-e

| Campo | Valor |
|---|---|
| nome | `NF-e` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Nota Fiscal Eletrônica |
| sinônimos | nota fiscal de produto |
| abreviações | NFE |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Fiscal |

### NFS-e

| Campo | Valor |
|---|---|
| nome | `NFS-e` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Nota Fiscal de Serviço Eletrônica |
| sinônimos | nota fiscal de serviço |
| abreviações | NFSE |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Fiscal |

### Pagamento

| Campo | Valor |
|---|---|
| nome | `Pagamento` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Liquidação de pagamento |
| sinônimos | baixa de pagamento, desembolso |
| abreviações | PAG |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Financeiro |

### RecebimentoFinanceiro

| Campo | Valor |
|---|---|
| nome | `RecebimentoFinanceiro` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Recebimento financeiro |
| sinônimos | baixa de recebimento, entrada financeira |
| abreviações | RCF |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Financeiro |

### CentroCusto

| Campo | Valor |
|---|---|
| nome | `CentroCusto` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Centro de Custo |
| sinônimos | unidade de custo, alocação |
| abreviações | CC |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Financeiro |

### ContaReceber

| Campo | Valor |
|---|---|
| nome | `ContaReceber` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Conta a Receber |
| sinônimos | título a receber, recebível |
| abreviações | CR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Financeiro |

### ContaPagar

| Campo | Valor |
|---|---|
| nome | `ContaPagar` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Conta a Pagar |
| sinônimos | título a pagar, obrigação |
| abreviações | CP |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Financeiro |

### Funcionário

| Campo | Valor |
|---|---|
| nome | `Funcionário` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Colaborador |
| sinônimos | empregado, membro interno |
| abreviações | FUNC |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | RH |

### Técnico

| Campo | Valor |
|---|---|
| nome | `Técnico` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Técnico de campo |
| sinônimos | executor técnico, especialista técnico |
| abreviações | TEC |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Assistência |

### Supervisor

| Campo | Valor |
|---|---|
| nome | `Supervisor` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Supervisor operacional |
| sinônimos | líder, coordenador |
| abreviações | SUP |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Instalação |

### Comprador

| Campo | Valor |
|---|---|
| nome | `Comprador` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Analista de compras |
| sinônimos | buyer, responsável de compras |
| abreviações | COMP |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Compras |

### Vendedor

| Campo | Valor |
|---|---|
| nome | `Vendedor` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Consultor comercial |
| sinônimos | representante comercial, executivo comercial |
| abreviações | VEND |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Comercial |

### Engenharia

| Campo | Valor |
|---|---|
| nome | `Engenharia` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Área de engenharia |
| sinônimos | time técnico, departamento técnico |
| abreviações | ENG |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Engenharia |

### Financeiro

| Campo | Valor |
|---|---|
| nome | `Financeiro` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Área financeira |
| sinônimos | tesouraria, contas |
| abreviações | FIN |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Financeiro |

### Jurídico

| Campo | Valor |
|---|---|
| nome | `Jurídico` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Área jurídica |
| sinônimos | legal, contratos |
| abreviações | JUR |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | Jurídico |

### RH

| Campo | Valor |
|---|---|
| nome | `RH` |
| descrição | Conceito ontológico genérico reutilizável. Será preenchido posteriormente com definição oficial da VerticalParts. |
| aliases | Recursos Humanos |
| sinônimos | people, gestão de pessoas |
| abreviações | RH |
| atributos principais | `id`, `codigo`, `nome`, `status`, `data_criacao`, `data_atualizacao` |
| relacionamentos | Ver `relationships.yaml` e `RELATIONSHIP_MODEL.md`. |
| estado de vida | Ver `states.yaml` e `STATE_MODEL.md` quando aplicável. |
| documentos relacionados | Será preenchido posteriormente. |
| KPIs relacionados | Será preenchido posteriormente. |
| responsável | Será preenchido posteriormente. |
| departamento | RH |
