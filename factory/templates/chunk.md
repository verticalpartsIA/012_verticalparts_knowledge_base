---
source_path: "{{ source_path }}"
chunk_id: "{{ chunk_id }}"
service: "{{ service }}"
method: "{{ method }}"
endpoint: "{{ endpoint }}"
focus: "{{ focus }}"
status: "{{ status }}"
embedding_version: 1
---
# Chunk {{ method }} - {{ focus }}

{{ content }}

